import React from 'react';

const Blockchain = ({ blockchain }) => {

  console.log(blockchain)
  return (
    <div>
      Blockchain
      {/* blockchain */}
    </div>
  )
}

export default Blockchain;
